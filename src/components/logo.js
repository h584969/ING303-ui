const Logo = () => {
    return(
        <svg viewBox="0 0 387 145" className="app-logo-container">
            <text x={0} y={145-42} className="app-logo">
                “Logo”
            </text>
        </svg>
    )
}

export default Logo;